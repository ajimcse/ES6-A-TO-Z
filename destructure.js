const person ={
    name:'ajim',
    age:25,
    crounty:'Bangladesh'
}
const {age} = person
console.log(age)


 const numbers = [10, 20, 30];

 const first = numbers[0];
 const third = numbers[2];

 console.log(first, third);